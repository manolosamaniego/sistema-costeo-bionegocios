stages:
  - deploy

pages:
  stage: deploy
  image: mcr.microsoft.com/powershell:7.4-ubuntu-22.04
  script:
    - pwsh -ExecutionPolicy Bypass -File ./scripts/preparar-public-web.ps1 -OutputDir public
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
